# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env
.env.local
.env.*
!.env.example
# gemini-cli settings
.gemini/

# GitHub App credentials
gha-creds-*.json
# Python
__pycache__/
*.py[cod]
# Vercel build output (config file vercel.json stays tracked)
.vercel

# TypeScript build artifact (tsc --incremental / tsBuildInfoFile)
*.tsbuildinfo

# Yabancı lockfile — proje npm kullanır (package-lock.json kanonik).
# Bu ortamda yanlışlıkla koşulan `bun install` artığı; commit edilmez.
bun.lock

# Geçici canlı test script'leri (commit edilmez)
scripts/test-mood-live.ts
scripts/test-openrouter-live.ts
.claude/
